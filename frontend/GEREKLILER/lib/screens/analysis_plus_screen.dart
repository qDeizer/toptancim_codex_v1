import 'package:flutter/material.dart';

class AnalysisPlusScreen extends StatelessWidget {
  const AnalysisPlusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Analiz+')),
      body: const Center(
        child: Text('Yapayzeka destekli analiz Yakında ...'),
      ),
    );
  }
}
